name: Cppcheck

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: get_cppcheck
      run: gcc actity1.c -o activity1.exe
    - name: Code Quality
      run: make src.c
